# cpkit

Reusable control-plane framework code extracted from `cp`.

This package is intentionally limited to generic mechanics. Product-specific
domains, request models, event catalogs, and lifecycle behavior stay in the
applications that consume it.
